{
  "entities": {
    "AdSubscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AdSubscription",
      "type": "object",
      "description": "Represents an ad subscription for a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ad subscription."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User. (Relationship: User 1:N AdSubscription)"
        },
        "adCampaignId": {
          "type": "string",
          "description": "Reference to the AdCampaign. (Relationship: AdCampaign 1:N AdSubscription)"
        },
        "startDate": {
          "type": "string",
          "description": "The date the subscription starts.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The date the subscription ends.",
          "format": "date-time"
        },
        "subscriptionType": {
          "type": "string",
          "description": "The duration of the subscription (monthly, quarterly, yearly)."
        },
        "price": {
          "type": "number",
          "description": "The price of the ad subscription."
        }
      },
      "required": [
        "id",
        "userId",
        "adCampaignId",
        "startDate",
        "endDate",
        "subscriptionType",
        "price"
      ]
    },
    "AdCampaign": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AdCampaign",
      "type": "object",
      "description": "Represents an ad campaign and its pricing on a community website.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ad campaign."
        },
        "websiteId": {
          "type": "string",
          "description": "Reference to the CommunityWebsite. (Relationship: CommunityWebsite 1:N AdCampaign)"
        },
        "monthlyPrice": {
          "type": "number",
          "description": "The monthly price for the ad campaign."
        },
        "quarterlyPrice": {
          "type": "number",
          "description": "The quarterly price for the ad campaign."
        },
        "yearlyPrice": {
          "type": "number",
          "description": "The yearly price for the ad campaign."
        },
        "description": {
          "type": "string",
          "description": "A description of the ad campaign."
        }
      },
      "required": [
        "id",
        "websiteId",
        "monthlyPrice",
        "quarterlyPrice",
        "yearlyPrice"
      ]
    },
    "DiscountOffer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "DiscountOffer",
      "type": "object",
      "description": "Represents a discount offer for ad subscriptions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the discount offer."
        },
        "name": {
          "type": "string",
          "description": "The name of the discount offer."
        },
        "description": {
          "type": "string",
          "description": "A description of the discount offer."
        },
        "discountPercentage": {
          "type": "number",
          "description": "The discount percentage (e.g., 10 for 10%)."
        },
        "startDate": {
          "type": "string",
          "description": "The date the discount offer starts.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The date the discount offer ends.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "discountPercentage",
        "startDate",
        "endDate"
      ]
    },
    "CommunityWebsite": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CommunityWebsite",
      "type": "object",
      "description": "Represents a community website on which ads can be placed.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the community website."
        },
        "name": {
          "type": "string",
          "description": "The name of the community website."
        },
        "url": {
          "type": "string",
          "description": "The URL of the community website.",
          "format": "uri"
        },
        "description": {
          "type": "string",
          "description": "A description of the community website."
        }
      },
      "required": [
        "id",
        "name",
        "url"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "role": {
          "type": "string",
          "description": "The role of the user (e.g., 'user', 'admin').",
          "enum": ["user", "admin"]
        },
        "stripeId": {
            "type": "string",
            "description": "The user's Stripe Customer ID."
        },
        "stripeLink": {
            "type": "string",
            "description": "A link to the user's Stripe customer portal."
        },
        "businessName": {
            "type": "string",
            "description": "The user's business name."
        },
        "contactName": {
            "type": "string",
            "description": "The user's full name."
        },
        "phone": {
            "type": "string",
            "description": "The user's phone number."
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product in Stripe.",
      "properties": {
        "id": { "type": "string" },
        "active": { "type": "boolean" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "role": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "tax_code": { "type": "string" }
      }
    },
    "Price": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Price",
      "type": "object",
      "description": "Represents a price in Stripe.",
      "properties": {
        "id": { "type": "string" },
        "product": { "type": "string" },
        "active": { "type": "boolean" },
        "description": { "type": "string" },
        "unit_amount": { "type": "number" },
        "currency": { "type": "string" },
        "type": { "type": "string" },
        "interval": { "type": "string" },
        "interval_count": { "type": "number" },
        "trial_period_days": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Path-based ownership enforced.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/adSubscriptions/{adSubscriptionId}",
        "definition": {
          "entityName": "AdSubscription",
          "schema": {
            "$ref": "#/backend/entities/AdSubscription"
          },
          "description": "Stores ad subscriptions for each user. Includes denormalized 'userId' for authorization independence and ownership enforcement.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "adSubscriptionId",
              "description": "The unique identifier for the ad subscription."
            }
          ]
        }
      },
      {
        "path": "/communityWebsites/{communityWebsiteId}",
        "definition": {
          "entityName": "CommunityWebsite",
          "schema": {
            "$ref": "#/backend/entities/CommunityWebsite"
          },
          "description": "Stores community website information. Globally readable.",
          "params": [
            {
              "name": "communityWebsiteId",
              "description": "The unique identifier for the community website."
            }
          ]
        }
      },
      {
        "path": "/communityWebsites/{websiteId}/adCampaigns/{adCampaignId}",
        "definition": {
          "entityName": "AdCampaign",
          "schema": {
            "$ref": "#/backend/entities/AdCampaign"
          },
          "description": "Stores ad campaigns for each community website. Admin-controlled.",
          "params": [
            {
              "name": "websiteId",
              "description": "The unique identifier for the community website."
            },
            {
              "name": "adCampaignId",
              "description": "The unique identifier for the ad campaign."
            }
          ]
        }
      },
      {
        "path": "/discountOffers/{discountOfferId}",
        "definition": {
          "entityName": "DiscountOffer",
          "schema": {
            "$ref": "#/backend/entities/DiscountOffer"
          },
          "description": "Stores discount offers. Globally readable, admin-controlled for creation/modification.",
          "params": [
            {
              "name": "discountOfferId",
              "description": "The unique identifier for the discount offer."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "adminRole",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Collection used to store admin roles. Existence of document indicates admin privileges.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": { "$ref": "#/backend/entities/Product" },
          "description": "Stripe products. Read-only for clients."
        }
      },
      {
        "path": "/products/{productId}/prices/{priceId}",
        "definition": {
          "entityName": "Price",
          "schema": { "$ref": "#/backend/entities/Price" },
          "description": "Stripe prices for products. Read-only for clients."
        }
      },
      {
        "path": "/customers/{userId}/checkout_sessions/{sessionId}",
        "definition": {
          "schema": { "type": "object" },
          "description": "Client writes to this collection to initiate a Stripe checkout session."
        }
      },
      {
        "path": "/customers/{userId}/subscriptions/{subscriptionId}",
        "definition": {
          "schema": { "type": "object" },
          "description": "Stripe subscriptions. Read-only for clients."
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to adhere to the principles of Authorization Independence, Clarity of Intent, DBAC, and QAPs, ensuring secure and scalable data management for the Community-Websites application. It leverages structural segregation and denormalization to optimize security rules and database operations.\n\n*   **Users:** User data is stored under `/users/{userId}`. This path-based ownership model simplifies security rules and provides clear ownership.\n*   **Ad Subscriptions:** Ad subscriptions are stored under `/users/{userId}/adSubscriptions/{adSubscriptionId}`. This structure maintains a clear 1:N relationship between users and their ad subscriptions. The `userId` field within the `adSubscriptions` collection enforces the ownership invariant.\n*   **Community Websites:** Community websites are stored in the `/communityWebsites/{communityWebsiteId}` collection. This allows for global access to website information without requiring authorization checks, given that this information is intended to be public. If websites had owners or restricted data, a different approach would be taken.\n*   **Ad Campaigns:** Ad campaigns are stored under `/communityWebsites/{websiteId}/adCampaigns/{adCampaignId}`. This maintains the 1:N relationship between community websites and ad campaigns. This structure ensures that only admins can modify ad campaigns, which is checked via a separate admin roles collection.\n*   **Discount Offers:** Discount offers are stored under `/discountOffers/{discountOfferId}`. Like community websites, discount offers are intended to be managed by administrators, and their collection is globally accessible.\n*   **Admin Roles:** Admin roles are managed through the `/roles_admin/{userId}` collection. The existence of a document in this collection grants admin privileges. This follows the Existence over Content strategy, simplifying role-based access control.\n*   **Stripe Integration**: Added collections for Stripe payments (`/products`, `/customers`) as required by the `firestore-stripe-payments` extension."
  }
}
